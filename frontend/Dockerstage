FROM node:latest AS svelte

COPY --chown=node:node $PACKAGE_DIR /home/node/frontend

WORKDIR /home/node/frontend
ENV VITE_OFRAK_VERSION=$OFRAK_VERSION
RUN su node -c "npm install && VITE_OFRAK_VERSION=${VITE_OFRAK_VERSION} npm run build"
